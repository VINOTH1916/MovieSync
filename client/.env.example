# Copy this to .env.local for local development
# For production (Render), set VITE_SERVER_URL in the Render dashboard

# Your deployed backend URL (set on Render)
VITE_SERVER_URL=https://your-backend.onrender.com
